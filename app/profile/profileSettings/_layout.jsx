import { Stack } from "expo-router";

const ProfileSettingsLayout = () => {
  return (
    <Stack
      screenOptions={{
        headerShown: false,
      }}
    />
  );
};

export default ProfileSettingsLayout;
